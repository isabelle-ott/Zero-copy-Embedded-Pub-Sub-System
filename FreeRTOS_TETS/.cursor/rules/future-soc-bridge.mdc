---
description: 面向未来 MCU + Linux SoC 桥接的规则：跨端协议、握手、spidev 过渡与可演进性
alwaysApply: true
---
# MCU / Linux SoC 跨端演进规则

虽然当前仓库主要是 STM32F4 侧工程，但本项目的设计目标明确包含未来 `i.MX6ULL Linux` 侧接入。因此在提出实现方案时，要主动为跨端一致性留接口。

## 跨端一致性
- MCU 侧与未来 Linux 侧都要朝统一的 `publish/subscribe` 抽象演进。
- 协议字段、Topic 命名、QoS 语义、错误码含义应尽量跨端一致。
- 不要把协议设计写死在某个 RTOS 或 HAL 细节里。

## SoC 侧分阶段策略
- 优先支持用户规划中的路线：先用 `spidev` 跑通端到端，再评估字符设备驱动、DMA 或更深度的零拷贝方案。
- 提建议时，不要默认一上来就要求写 Linux 内核驱动。
- 若给出 Linux 侧建议，要显式提醒：GPIO 中断上半部尽量短，耗时逻辑放下半部/工作队列。

## 传输与鲁棒性
- SPI + GPIO 握手的设计要把 Linux 非实时调度视为事实，而不是异常。
- MCU 侧要预留 ACK 超时、重试、降级和 QoS 缓冲策略。
- 对于 OTA、日志、传感器等不同 Topic，方案上要体现优先级差异。

## 设计表达
- 回答和代码建议都要体现“先跑通保底链路，再做性能/零拷贝优化”的路线。
- 如果用户的需求可能把项目拖入过度复杂实现，应优先建议分阶段落地。
