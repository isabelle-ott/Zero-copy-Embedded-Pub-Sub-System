---
description: 全局项目规则：异构 Pub/Sub、中文输出、分层边界、保守改动策略
alwaysApply: true
---
# Micro-PubSub 项目全局规则

这是一个 `STM32F4 + FreeRTOS` 实时侧工程，目标架构将演进为 `STM32F4 (MCU) + i.MX6ULL (Linux SoC)` 的异构发布订阅中间件。你在此项目中工作时，必须优先遵守以下约束。

## 交流与工作方式
- 默认使用中文回答。
- 优先做“小而准”的改动，避免大面积重构。
- 先理解现有目录职责，再动手修改；不要把驱动、业务、日志、协议逻辑混写在一起。
- 对用户的规划保持对齐：当前保底目标优先级高于“过度设计”。先让链路跑通，再做零拷贝、驱动优化、UE5 可视化等增强项。

## 架构认知
- 该项目核心目标不是单纯点亮外设，而是构建“总线无关、跨 FreeRTOS 与 Linux 的轻量级 Pub/Sub 中间件”。
- 任何实现都应服务于以下主线：静态内存池、Topic 路由、SPI + GPIO 握手、双 OTA、双缓冲日志、跨端一致的 publish/subscribe 抽象。
- 当实现细节与项目目标冲突时，优先保留：可预测性、可验证性、分层清晰度、资源约束友好性。

## 编码原则
- 优先使用静态分配；不要随意引入 `malloc/free` 或不可控堆分配。
- 优先保证可预测内存占用、确定性时延、可恢复错误路径。
- 嵌入式路径上避免异常复杂的宏技巧、隐式副作用和不必要的抽象层。
- 涉及位图、索引、帧格式、DMA 缓冲时，显式处理边界条件、对齐和长度检查。
- 新增 API 时，命名尽量贴合已有风格，并体现模块边界。

## 分层边界
- `Core/` 主要承载 CubeMX 生成代码、启动、外设初始化、中断入口及板级 glue code。
- `app/` 主要放任务编排、业务流程、状态机、Topic 级应用逻辑。
- `common/` 主要放可复用模块，例如日志、协议、内存池、路由、传输抽象等。
- `Drivers/`、`Middlewares/`、`FATFS/` 视为第三方或自动生成基础设施；除非确有必要，不要主动改库源码。
- `build/` 下内容视为构建产物，默认不手改。

## 与项目规划强绑定的约束
- 要始终记住 STM32F4 上 `CCM RAM` 不能给 DMA 访问；DMA Buffer 必须放可 DMA 访问的 SRAM 区域。
- 使用 `__builtin_clz()` 或同类位操作时，必须先处理输入为 `0` 的情况。
- 结构化协议帧若跨端传输，必须关注 `packed`、对齐、字节序、长度字段和 CRC/校验字段的一致性。
- SPI 帧边界优先依赖硬件握手或片选，不要退化成脆弱的软件帧头方案。
- QoS 行为必须与 Topic 类型绑定，不能把所有消息简单等同处理。

## 生成与验证
- 修改后优先检查是否破坏现有生成代码结构、头文件依赖和模块职责。
- 若改动涉及并发、DMA、中断、任务同步、文件系统刷盘，必须主动检查竞态、阻塞时间和错误恢复路径。
- 给出的实现建议应尽量可落地到当前代码库，而不是停留在抽象描述。
