---
description: app 与 common 规则：任务、日志、Pub/Sub 中间件、静态内存与模块边界
globs: {app/**,common/**}
alwaysApply: false
---
# app / common 模块规则

这些目录承载项目真正的业务价值：任务编排、日志模块、Pub/Sub 抽象、内存池、路由、传输适配和未来 OTA 能力。修改时要优先保证模块职责清晰。

## 目录职责
- `app/`：任务创建、任务函数、状态机编排、模块装配、应用层 Topic 使用。
- `common/`：可复用组件，例如日志、协议帧、内存池、路由、传输抽象、公共工具函数。
- 若某段逻辑未来可能同时被多个任务或模块复用，应优先放 `common/`。

## FreeRTOS 任务设计
- 不要在任务函数里堆满初始化、业务逻辑、错误处理和硬件细节；应拆成清晰的辅助函数。
- 谨慎使用阻塞式 API；必须明确阻塞时间是否会影响实时性。
- 任务间通信优先使用明确的同步原语或消息机制，不要依赖隐式全局状态。
- 若日志或 OTA 分片具有不同 QoS，逻辑上要体现“丢弃 / 覆盖 / 阻塞”的差异，而不是统一处理。

## 日志系统专项要求
- 日志模块目标是“双缓冲 + 批量刷盘 + 降低抖动”，不是每条日志都立刻同步写文件。
- 避免在普通日志路径里频繁 `f_sync()`；应倾向批量提交或按策略同步。
- 若当前实现仍处于 MVP 阶段，可接受先跑通基础写盘，但后续设计必须为 Ping-Pong Buffer 预留清晰扩展点。
- 记录日志时要考虑：缓冲区长度、并发访问、刷盘失败、文件未挂载、写满或卡慢等异常情况。

## Pub/Sub 与协议抽象
- 接口设计要朝 `publish(topic, data)` / `subscribe(topic, callback)` 的统一抽象靠拢。
- Topic、帧头、载荷、QoS、元数据之间的边界必须明确，避免互相耦合。
- 结构化帧定义时，必须显式描述长度、类型、序号、校验字段及跨端兼容性。

## 静态内存池与零拷贝
- 该项目强依赖静态内存池；默认不要引入动态内存分配替代方案。
- 位图管理、块回收、引用计数或指针转移等逻辑必须可推理、可验证。
- 若使用位扫描优化分配路径，必须显式防守空位图输入。
- 零拷贝的前提是生命周期清晰；如果订阅者可能异步持有数据，必须设计好 ownership 规则。

## 可维护性
- 头文件只暴露必要接口，避免把实现细节和第三方依赖泄露给所有模块。
- 公共模块 API 要有明确错误返回或状态语义，不要大量依赖 silent failure。
- 若代码仍在快速演进，允许保留 TODO，但 TODO 必须具体，能指导下一步实现。
