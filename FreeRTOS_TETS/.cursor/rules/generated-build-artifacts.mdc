---
description: 生成产物与第三方代码规则：默认不手改 build 和库源码
globs: {build/**,Middlewares/Third_Party/**,Drivers/STM32F4xx_HAL_Driver/**}
alwaysApply: false
---
# 生成产物 / 第三方代码规则

这些目录通常不是业务逻辑的最佳落点。处理时要非常克制。

## 默认策略
- `build/` 下的文件默认视为生成产物，不要手工修改。
- `Middlewares/Third_Party/` 与 `Drivers/STM32F4xx_HAL_Driver/` 默认视为第三方源码，不要因为局部需求直接侵入修改。
- 若确实需要调整行为，优先寻找配置层、封装层、适配层或用户代码入口。

## 允许修改的例外情形
- 用户明确要求修改第三方源码。
- 当前问题只能通过库源码补丁解决，且必须在回答中明确说明风险和影响范围。
- 变更属于 CubeMX / 中间件集成缺失导致的必要补充，而非个人风格优化。

## 处理原则
- 优先最小改动。
- 修改第三方或生成文件前，先确认是否能通过 `app/`、`common/`、配置宏、回调钩子或外层包装解决。
- 若变更会在重新生成后丢失，应明确提示用户该风险。
