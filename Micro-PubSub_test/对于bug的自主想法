问题1：
mem_pool.c — 问题
问题1：mps_free 双重释放检测逻辑有根本性缺陷
c// 当前代码
if ((pool->bitmap & (1u << idx)) == 0) {
    // 块在使用中，执行引用计数递减
} else {
    // 块已空闲，返回 MPS_ERR_INVALID
}
这个逻辑有一个严重语义错误：当 ref_count 已经是 0 但块还没被回收（正在回收过程中）的中间状态不存在，但更重要的是——一个订阅者调用了两次 mps_free 会发生什么？
第一次调用：ref_count 从1变0，bitmap 位被置1（块回收）。
第二次调用：bitmap 位是1（空闲），进入 else 分支，返回 MPS_ERR_INVALID。
这部分看起来正确，但是——如果另一个 mps_alloc 在两次 free 之间抢到了这个 block，第二次 free 就会把一个正在使用的 block 错误释放掉，因为 _ptr_to_idx 返回的 idx 是合法的，且 bitmap 位此时是0（已被新分配占用），所以会进入 if 分支并递减新 block 的 ref_count。
这是一个 use-after-free 类的竞态漏洞。 在 FreeRTOS 多任务环境下，mps_free 和 mps_alloc 之间没有任何临界区保护（没有 mutex / taskENTER_CRITICAL）。整个内存池是全局共享的，任何并发访问都是 undefined behavior。
想法：针对这个问题，能不能在两次free之前加上 taskENTER_CRITICAL，在之后加上taskEXIT_CRITICAL，确保没被抢占

问题2：mps_alloc 的 ref_count 初始化位置错误
uint32_t idx = 31 - __builtin_clz(pool->bitmap);
pool->bitmap &= ~(1u << idx);
pool->ref_count[idx] = 1;  // ← 在清除 bitmap 之后才设置
在单线程下没问题。但逻辑顺序应该是先设 ref_count，再清 bitmap，否则在中断抢占的环境下，另一个任务可能看到 bitmap 位已清（块"在用"）但 ref_count 还是0的中间状态。
想法：
uint32_t idx = 31 - __builtin_clz(pool->bitmap); // 1. 先算索引
pool->ref_count[idx] = 1;                        // 2. 再设引用计数
pool->bitmap &= ~(1u << idx);                    // 3. 最后清空位图占坑

问题3：问题3：mps_free 中对 ref_count == 0 的块调用 mps_free 没有防御
cif (pool->ref_count[idx] > 0) {
    pool->ref_count[idx]--;
}
if (pool->ref_count[idx] == 0) {
    pool->bitmap |= (1u << idx);
}
如果有人对一个 ref_count 已经是0、但 bitmap 位还是0（这不应该发生，但防御性编程就是要堵这个）的 block 调用 mps_free，会静默地把 bitmap 位置1（"回收"一个 ref_count=0 的块）。这种状态根本不应该存在，遇到了应该 assert 或返回错误，而不是静默通过。

想法：修改成下面
    if ((pool->bitmap & (1u << idx)) == 0) {
        
        // [修改核心逻辑]：递减引用计数
        if (pool->ref_count[idx] > 0) {
            pool->ref_count[idx]--;
        }
        
        // 只有当所有订阅者和发布者都调用了 free，计数归 0 时，才真正回收物理内存块
        if (pool->ref_count[idx] == 0) {
            if(pool->bitmap == 0)
            {
                return MPS_ERR_INVALID;
            }
            pool->bitmap |= (1u << idx);
        }
        
        return MPS_OK;

问题4：
问题4：Topic_Publish 的内存所有权语义在发布失败时有内存泄漏
cbool Topic_Publish(const char* topic_name, const void* payload) {
    if (topic_name == NULL || payload == NULL || g_topic_mem_pool == NULL) return false;
    // ...
    if (topic_index == -1) return false;  // ← 直接 return，没有 free payload！
调用者用 Topic_AllocPayload 拿到 payload（此时 ref_count = 1），然后调用 Topic_Publish，如果 topic 找不到，函数直接 return false。payload 永远不会被 free，这是一个内存泄漏。
按你自己的设计（.h 文件里写了"发布者必须主动调用 mps_free"），如果 Publish 失败，调用者根本不知道该不该 free。这个所有权语义没有在 API 里明确表达，是一个设计漏洞。
正确做法：无论 Publish 成功失败，函数内部都应该调用 mps_free 来剥离发布者的所有权，调用者永远不应该在 Publish 之后还持有 payload。

想法：为了让系统仍然保持极简状态：在用户协议里面强调，不论是谁执行=了payroad,最后都必须要mps_free

问题5：
问题5：Topic_Publish 的 payload 参数类型与 mps_free 签名冲突
cbool Topic_Publish(const char* topic_name, const void* payload) {
    // ...
    mps_free(g_topic_mem_pool, (void*)payload);  // ← 强制去掉 const
你把 const void* 强制转成 void* 来调用 mps_free。这说明 Topic_Publish 的参数签名设计是错的——既然 Publish 要释放这块内存，它就不应该接受 const void*。接受 const 指针却在内部修改（释放）它，是一个语义谎言，会掩盖调用者的错误使用。
应该改成 void* payload，或者设计一个单独的 Topic_PublishAndRelease 明确表达所有权转移。
想法：bool Topic_Publish(const char* topic_name, void* payload) 

问题6：
问题6：Topic_AllocPayload 的 QOS_CTRL 分支可能永久阻塞
ccase QOS_CTRL:
    vTaskDelay(pdMS_TO_TICKS(1));
    continue;
你在注释里承认了这个问题，但没有解决它——这个 while(1) 没有超时上限。在内存池被打满且没有任何消费者释放的情况下（比如所有订阅者 callback 里忘了 free），这个任务会永远挂起，直到 RTOS watchdog 触发。这在生产代码里不可接受，必须有最大重试次数或超时时间。
c// 需要改成类似这样：
int retry = 0;
while (retry < MAX_CTRL_RETRY) {
    ptr = mps_alloc(g_topic_mem_pool);
    if (ptr != NULL) return ptr;
    vTaskDelay(pdMS_TO_TICKS(1));
    retry++;
}
return NULL;  // 超时后返回 NULL，让上层决策
之后阶段再尝试升级Qos算法


问题8：test_PublishDispatch 测试设计有根本缺陷
cvoid test_PublishDispatch(void) {
    int test_data = 1024;
    // ...
    bool pub_result = Topic_Publish("/test/data", &test_data);
test_data 是栈上的局部变量，你直接把它的地址传给 Topic_Publish。但 Topic_Publish 内部会调用 mps_free(g_topic_mem_pool, (void*)payload)，而这个地址根本不在内存池的 storage 范围内，_ptr_to_idx 会返回 -1，mps_free 会返回 MPS_ERR_INVALID。
这个测试完全没有走正确的代码路径——它绕过了 Topic_AllocPayload，没有测试真实的零拷贝流程，也没有测试 ref_count 机制。测试通过了（pub_result 是 true），但它测的是一个错误的使用场景。

想法如下
void test_PublishDispatch(void) {
    int test_data = 1024;
    
    Topic_Register("/test/data", QOS_LOG);
    Topic_Subscribe("/test/data", DummyCallback1);
    Topic_Subscribe("/test/data", DummyCallback2);
    
    // 执行发布
    int *payload = (int*)Topic_AllocPayload("/test/data");
    *payload = 1024;
    bool pub_result = Topic_Publish("/test/data", payload);
    
    // 验证结果
    TEST_ASSERT_TRUE(pub_result);
    // 两个订阅者，所以应该被调用两次
    TEST_ASSERT_EQUAL(2, g_callback_call_count); 
    // 验证数据正确传递（零拷贝的指针传递效果）
    TEST_ASSERT_EQUAL(1024, g_mock_received_value);
}


问题9：g_callback_call_count 和 g_mock_received_value 没有在 setUp 中重置
两个全局变量在每次测试前不归零，如果测试顺序改变或添加新测试，会产生累积污染。setUp 里必须加：

想法：
void setUp(void) {
    g_mock_received_value = 0;
    g_callback_call_count = 0;
    mps_init(&test_pool);
    TopicTree_Init(&test_pool);
}

问题10：DummyCallback1 和 DummyCallback2 没有调用 mps_free
根据你在 topic_tree.h 里自己写的规定：

所有订阅者在消费完 payload 后，必须主动调用 mps_free(pool, payload)

但两个 Dummy callback 都没有调用 mps_free。这导致测试中每次 Publish 后，内存池都有泄漏（ref_count 没有归零）。在更多测试 case 运行后，池会被耗尽。测试本身就违反了你的设计契约。

想法如下
static MemPool_t test_pool;

/* 测试用的回调函数 */
void DummyCallback1(const void* payload) {
    if (payload != NULL) {
        g_mock_received_value = *(const int*)payload;
    }
    g_callback_call_count++;
    mps_free(&test_pool, (void*)payload); // 模拟订阅者消费完 payload 后调用 free
}

void DummyCallback2(const void* payload) {
    g_callback_call_count++; // 用于测试多订阅者分发
    mps_free(&test_pool, (void*)payload);
}

------------------------

依旧修改mps_free
现有问题：
时间线：
1. Task A: mps_free(ptr)          ← 第1次释放，bitmap bit → 1（空闲）
   [临界区正确退出]
2. Task B: mps_alloc() → 拿到同一块  ← bitmap bit → 0（占用），ref=1
3. Task A: mps_free(ptr)          ← 第2次释放，看到bit=0以为合法，ref-- → 0，误杀！
   [临界区正确退出]